# Настройка Telegram Bot API для отправки сообщений

## Шаг 1: Получите токен бота

1. Откройте @BotFather в Telegram
2. Выберите вашего бота `@giftdrawtoday_bot`
3. Выполните команду `/token`
4. Скопируйте токен бота

## Шаг 2: Добавьте переменную окружения в Vercel

1. Откройте ваш проект в Vercel Dashboard
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте новую переменную:
   - **Name**: `TELEGRAM_BOT_TOKEN`
   - **Value**: ваш токен бота (например: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)
   - **Environment**: Production, Preview, Development (выберите все)
4. Нажмите **Save**

## Шаг 3: Перезапустите деплой

После добавления переменной окружения:
1. Перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите **Redeploy** (или сделайте новый коммит в Git)

## Как это работает

- При открытии мини-апп автоматически открывается чат с ботом
- Запрашивается разрешение на отправку сообщений (`requestWriteAccess`)
- После открытия чата отправляется приветственное сообщение с кнопками:
  - 🎫 Купить билет
  - 📊 Мои билеты
  - 🏆 Текущий розыгрыш
  - 💰 Мой баланс
  - ❓ Помощь

## API Endpoint

Создан серверный endpoint `/api/send-message` для отправки сообщений:

**POST** `/api/send-message`

**Body:**
```json
{
  "chatId": 123456789,
  "text": "Текст сообщения",
  "buttons": [
    [
      { "text": "Кнопка 1", "callback_data": "action1" },
      { "text": "Кнопка 2", "callback_data": "action2" }
    ]
  ]
}
```

**Response:**
```json
{
  "success": true,
  "messageId": 123
}
```

## Использование в коде

В `MiniApp.tsx` есть функции:
- `sendMessageToBot(chatId, text, buttons)` - отправка сообщения с кнопками
- `sendWelcomeMessage(telegramId)` - отправка приветственного сообщения

Пример использования:
```typescript
await sendMessageToBot(
  123456789,
  "Ваше сообщение",
  [
    [{ text: "Кнопка", callback_data: "action" }]
  ]
);
```

